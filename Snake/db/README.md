# Database (Phase 3)

The schema is initialized from `init.sql`.

Tables:
- `users`
- `scores`

Security and performance:
- Passwords are hashed in PHP using `password_hash`.
- Leaderboard indexes are included on score and user/time dimensions.
