apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    {{- include "snake-stack.labels" . | nindent 4 }}
spec:
  selector:
    app: {{ include "snake-stack.fullname" . }}-frontend
  ports:
    - name: http
      port: 80
      targetPort: http
